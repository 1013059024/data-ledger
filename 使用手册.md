# 🛣️ 数据台账系统系统 · 使用手册

> 基于 Flask + jQuery + Bootstrap 5 的单页 Web 应用，MySQL 后端。

---

## 目录

1. [启动与关闭](#1-启动与关闭)
2. [表管理](#2-表管理)
3. [表格视图操作](#3-表格视图操作)
4. [单元格编辑](#4-单元格编辑)
5. [选中与复制粘贴](#5-选中与复制粘贴)
6. [右键菜单](#6-右键菜单)
7. [筛选与排序](#7-筛选与排序)
8. [列操作](#8-列操作)
9. [导入导出](#9-导入导出)
10. [字段映射与同步](#10-字段映射与同步)
11. [多表汇总](#11-多表汇总)
12. [分类汇总](#12-分类汇总)
13. [备份与恢复](#13-备份与恢复)
14. [键盘快捷键](#14-键盘快捷键)
15. [数据安全](#15-数据安全)

---

## 1. 启动与关闭

### 一键启动

```bash
python start.py
```

自动完成：初始化 MySQL → 启动 MySQL 服务 → 创建 `data_ledger` 库 → 启动 Flask（`127.0.0.1:5000`）。

### 批处理启动

双击 `start.bat`（仅执行 `python start.py`，不含中文避免编码问题）。

### 手动分步启动

```bash
# 1. 启动 MySQL
"C:\Program Files\MySQL\MySQL Server 8.4\bin\mysqld.exe" ^
  --datadir="E:\...\data-ledger\mysql-data" ^
  --console --port=3306

# 2. 启动 Flask
python server\app.py
```

### 关闭

- **Flask**：`Ctrl+C` 终止终端进程
- **MySQL**：`mysqladmin -u root shutdown` 或直接杀死进程

---

## 2. 表管理

### 新建表

1. 点击侧边栏 **「＋新建」**
2. 填写表名 → 添加字段（列名 + 数据类型）
3. 选择关键字段（唯一标识，用于跨表关联）
4. 点击「创建」

支持的字段类型：`VARCHAR`、`INT`、`DECIMAL`、`DOUBLE`、`DATE`、`TEXT`。

### 切换表

点击侧边栏表名即可切换。支持**分组折叠**：点击分组标题 ▶/▼ 展开或收起。

### 重命名表

点击表名旁的 **✏️** → 输入新名 → 确定。

### 删除表

点击 **🗑️** → 输入提示令牌（防误删）→ 确认删除。

> ⚠️ 删除前自动备份到 `server/_backup/`，可从回收站恢复。

### 回收站

点击侧边栏 **🗑️ 回收站** → 查看所有备份 → 勾选后批量恢复或删除。

---

## 3. 表格视图操作

### 工具栏按钮

| 按钮 | 功能 |
|------|------|
| 🔗 映射 | 管理表间字段映射 |
| 🗄 数据库 | 查看所有表统计 |
| 📤 导入 | 上传 Excel/CSV 导入数据 |
| 💾 备份 | 全库导出/导入 |
| 🔄 | 刷新当前表 |
| 🧹 去重 | 对关键字段查重，自动删除重复记录 |
| 📥 导出 | 下载当前表为 `.xlsx`（公文格式） |
| 📥 补填 | 从关联表自动补填空字段 |
| 📊 多表合一 | 合并多张表为汇总表 |
| 📋 分类汇总 | 分组统计生成新表 |
| 🧹 筛选(N) | 清除所有列筛选条件 |
| 📋 列功能 | 下拉菜单：排序/隐藏/筛选 |

### 搜索

顶部搜索框 → 输入关键词 → 400ms 后自动模糊搜索所有文本字段 → 匹配内容黄色高亮。

---

## 4. 单元格编辑

### 编辑方式

- **双击单元格** → 出现输入框 → 修改后 blur / Enter 保存
- **Tab** → 跳到下一格
- **Enter** → 跳到下一行（同列）
- **Esc** → 取消编辑，恢复原值

### 填充柄

选中单元格后右下角会出现 **■ 填充柄** → 拖拽到相邻单元格 → 同列批量填充（复制值）。

### 自动计算

如果列设置了公式（右键 → 📐 公式），编辑本行任意单元格后自动重算并保存公式结果。

---

## 5. 选中与复制粘贴

### 选中方式

| 操作 | 效果 |
|------|------|
| 单击单元格 | 选中单格，显示填充柄，清掉行/列选中 |
| 拖拽 | 矩形范围选中，清掉行/列选中 |
| Shift + 点击 | 从锚点扩展矩形选中 |
| Ctrl + 点击 | 切换选中/取消 |
| 点击列头 | 选中整列，清掉行/格选中 |
| 拖拽列头 | 多列选中（列头绿色 + 单元格浅蓝），清掉行/格选中 |
| Shift + 点击列头 | 范围多列选中 |
| Ctrl + 点击列头 | 切换单列选中 |
| 点击行号 | 选中整行（黄色高亮），清掉列/格选中 |
| 拖拽行号 | 多行选中（黄色高亮），清掉列/格选中 |

> **注意**：三种选中（单元格、列、行）互斥，执行一种选中会自动清除另外两种。

### 选中信息栏

表格左下角的灰色信息栏实时显示当前选中状态：

| 选中类型 | 显示内容 | 底色 |
|---------|---------|------|
| 单元格拖拽选中 | **N 行 × M 列，共 X 个单元格，总和 Y** | 深蓝 `#cce5ff` + 外轮廓 |
| 列头拖拽多列 | **N 行 × M 列，共 X 个单元格，总和 Y** | 列头绿色 + 单元格浅蓝 `#e8f4ff` |
| 行号拖拽多行 | **N 行已选中** | 黄色 `#fff3cd` |
| 无选中 | 隐藏 | — |

**取消选中**：点击表格外空白区域清除全部选中状态。

### 复制粘贴

| 快捷键 | 功能 |
|--------|------|
| **Ctrl+C** | 复制选中区域到本地缓存 |
| **Ctrl+V** | 粘贴到目标区域 |

**粘贴规则：**

- **目标选中 1 格（锚点）** → Excel 风格自动展开：从锚点向右/下展开源数据的行列数
- **目标选中多格** → 位置一一匹配（第 N 个源 → 第 N 个目标）
- **列头选中** → 按行 ID + 字段名匹配粘贴，不同名字段自动回退取首字段值
- 粘贴后自动刷新表格

### 跨表粘贴

- 服务端剪贴板模式（Ctrl+V 且无本地缓存时）→ 按关键字段匹配
- 匹配到行 → **更新**；未匹配到 → **新增行**；隐藏记录 → **自动恢复**

---

## 6. 右键菜单

### 表格主体右键（`#rowMenu`）

| 菜单项 | 说明 |
|--------|------|
| 📋 复制选中区域 | 复制选中的单元格到缓存 |
| 📝 粘贴 | 从缓存粘贴到目标区域 |
| 🧹 清除内容 | 清空选中单元格（不删行） |
| ⬇ 插入空行 | 在表末尾新增空行 |
| 📋 查看数据库记录 | 打开记录弹窗，查看所有字段 + 关联表数据 |
| 👁 从本表隐藏 | `_deleted=1`，可从右上角 **已隐藏 N 行 → 显示** 恢复 |
| 🗑 从本表删除 | `_deleted=2`，可从 **🗄 数据库** → 查看该表 → **↩ 恢复** |
| 🔥 从数据库删除 | 物理删除，**不可恢复**（含级联删除关联记录） |

> **批量操作**：右键菜单执行时，如果已有选中区域（拖拽 / Shift+点击），会自动将所选中的所有行一起处理。无需反复单个操作。

### 列头右键（`#colMenu`）

| 菜单项 | 说明 |
|--------|------|
| 🔑 设为核心字段 / 🚫 取消 | 设置/取消表的关键字段 |
| 🔧 修改数据类型 | 修改列的数据类型 |
| ➕ 插入列（右侧） | 新增一列 |
| 📝 调整字段顺序 | 打开排序弹窗，上移/下移 |
| ❄️ 冻结此列 / 取消冻结 | 冻结列在左侧，水平滚动保持可见 |
| 📐 公式 / 修改公式 | 设置计算公式（支持 `+ - * / ( )` 和字段名） |
| 🗑 删除本列 | 永久删除列及其数据 |

---

## 7. 筛选与排序

### 筛选

1. 点击列头右侧的 **▾ 图标**
2. 弹出筛选面板 → 勾选要显示的值
3. 点击「确定」→ 表格只显示匹配行
4. **级联筛选**：选中一列后，其他列的筛选选项自动缩小范围
5. 顶部 `#filterTags` 显示已激活的筛选标签，点击 ✕ 移除单个
6. **🧹 筛选(N)** 按钮一键清除所有筛选

### 排序

点击列头 → 三态切换：**不排序 → ↑ 升序 → ↓ 降序**。
跨表切换时保持排序设置。

---

## 8. 列操作

### 列功能菜单（📋 列功能）

下拉菜单中每行列包含操作：**排序**（↑↓）、**隐藏**（👁️/🚫）、**筛选**（▾）。

### 列隐藏

- 列功能菜单点击 👁️/🚫 切换
- 隐藏列从视图移除，数据保留
- 隐藏配置保存在 `_hidden_columns.json`

### 列冻结

- 右键列头 → ❄️ 冻结/取消冻结
- 冻结列固定在表格左侧，水平滚动时保持可见
- 多列冻结依次叠加偏移

### 公式列

- 右键列头 → 📐 公式 → 输入表达式
- 支持：`+` `-` `*` `/` `(` `)` 和字段名引用，如 `路面宽度 * 建设里程`
- 结果自动计算并保存到数据库
- 编辑本行任意单元格后自动重算

---

## 9. 导入导出

### 导入流程

1. 点击 **📤 导入** → 选择文件（`.xlsx` / `.xls` / `.csv` / `.et`）
2. 解析后选择 Sheet
3. 设置**表头行范围**（支持双层合并表头）
4. 勾选要导入的列
5. 设置**关键字段**（只导入该字段非空的行）
6. 预览数据 → 确认导入
7. 同名表自动加 `_1` / `_2` 后缀避免冲突

### 导出

点击 **📥 导出** → 下载 `.xlsx`，格式：
- 标题：黑体 16pt 加粗（使用表注释，如"项目总表"）
- 表头：黑体 10.5pt 加粗
- 正文：仿宋 10.5pt
- 全框线、居中对齐、自动列宽
- 打印设置自适应方向 + 冻结表头

> **所见即所得**：导出完全反映当前网页状态——
> - ✅ **筛选**：只导出激活筛选条件下匹配的行
> - ✅ **排序**：按当前排序方向导出
> - ✅ **隐藏列**：已隐藏的列不会出现在 Excel 中

---

## 10. 字段映射与同步

### 管理映射

1. 点击 **🔗 映射**
2. 「新建映射」→ 选择源表 / 目标表 → 自动匹配同名字段
3. 不同名字段显示 ⚠️ 标签，可「统一」重命名目标表字段
4. 编辑映射：点击 ✎ 修改字段对
5. 删除映射：点击 ✕

### 核实与补填

- **核实**：对比两表映射字段数据，显示不一致的条目
- **自动补填**：编辑关键字段时自动从关联表拉取空字段数据
- **批量补填**：点击 **📥 补填** → 扫描所有空映射字段并填充

### 汇总表自动映射

创建汇总表后自动生成 ↔ 各源表的映射，汇总表编辑结果通过映射同步回源表。

---

## 11. 多表汇总

1. 点击 **📊 多表合一**
2. 勾选 ≥2 张源表 → 选择公共关键字段
3. 自动预览（前 100 行）
4. **字段冲突**：同义不同名的字段弹窗让你手动统一列名
5. **孤立字段**：每张表独有的字段单独分列
6. 点击「创建汇总表」→ 生成新表 + 自动建立映射关系

汇总表双击单元格可编辑，通过映射自动同步回源表。

---

## 12. 分类汇总

1. 点击 **📋 分类汇总**
2. 选择**分组字段**（如"乡镇"）
3. 添加**汇总项**：选择汇总方式（计数/求和）+ 字段 + 可选别名
4. 点击「创建」→ 生成新表

支持多个汇总项同时计算。

---

## 13. 备份与恢复

### 查重（去重）

点击 **🧹 去重** → 对当前表的关键字段扫描 → 发现重复值时自动删除（只保留 ID 最小的记录）→ 显示删除条数。操作前有确认弹窗。

> 注意：去重前请确保关键字段已设置（🔑 显示不为"—"）。

### 自动备份

- **删表前**自动备份数据和结构到 `server/_backup/<表名>_<时间戳>.json`
- **建表/导入后**更新 `_manifest.json`

### 手动全库备份

1. 点击 **💾 备份** → 「导出」
2. 下载全库 `.json`（所有表结构 + 数据 + 配置）

### 全库恢复

1. 点击 **💾 备份**
2. 「导入」→ 选择 `.json` 文件
3. 自动覆盖恢复整个数据库

### 回收站恢复

侧边栏 **🗑️ 回收站** → 勾选要恢复的表备份 → 「恢复选中」。

---

## 14. 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+C` | 复制选中区域 |
| `Ctrl+V` | 粘贴 |
| `Tab` | 编辑模式：跳到下一格 |
| `Enter` | 编辑模式：跳到下一行 / 保存 |
| `Esc` | 编辑模式：取消编辑 |
| `Delete` | 选中区域有填充柄时清除内容 |

---

## 15. 数据安全

- **防误删保护**：API 删除端点需要 `del_token` 参数匹配服务端随机令牌，前端脚本无法通过 API 直接删除表
- **删表自动备份**：任何表删除前自动备份到 `_backup/`
- **隐藏而非删除**：行隐藏（_deleted=1）优先于硬删除，可从数据库总览恢复查看
- **关键字段保护**：关键字段为空的行不允许硬删除（先隐藏）
- **级联删除**：删除行时自动清理关联表的外键引用数据

---

> 最后更新：2026-05-30（v2.1 — 选中互斥、行选中信息栏、三种选中视觉分离）
